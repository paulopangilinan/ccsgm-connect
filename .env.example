# Copy to .env and fill in from Supabase Project Settings -> API.
# SUPABASE_ANON_KEY is safe to expose client-side; RLS in supabase/schema.sql is the real access boundary.
SUPABASE_URL=
SUPABASE_ANON_KEY=
