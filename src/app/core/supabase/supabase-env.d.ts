// Replaced with string literals at build time via esbuild `define`
// (see scripts/ng-with-env.mjs) — never read from `process.env` at runtime.
declare const SUPABASE_URL: string;
declare const SUPABASE_ANON_KEY: string;
